import React from 'react';

export default function InfoModal({ isOpen, onClose }) {
  if (!isOpen) return null;

  return (
    <div className="modal-overlay" onClick={onClose}>
      <div className="modal-card" onClick={(e) => e.stopPropagation()}>
        <div className="modal-header">
          <div style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
            <span style={{ fontSize: '1.4rem' }}>🔬</span>
            <div>
              <h2 className="modal-title">Methodology, Tanimoto & Abstention</h2>
              <span style={{ fontSize: '0.78rem', color: 'var(--text-dim)' }}>
                Track 3 Technical Principles & Domain Safeguards
              </span>
            </div>
          </div>
          <button className="card-close-btn" onClick={onClose}>✕</button>
        </div>

        <div className="modal-body">
          {/* Tanimoto Section */}
          <div className="callout-box">
            <div className="callout-title">
              <span>💡</span> Why do some compounds show Tanimoto Similarity = 1.00?
            </div>
            <p>
              <strong>Nearest Training Tanimoto Similarity</strong> compares the 1024-bit Morgan fingerprint 
              of the input molecule against all <strong>1,572 compounds in the Training Set</strong>.
            </p>
            <ul style={{ paddingLeft: '20px', lineHeight: '1.6' }}>
              <li>
                <strong>Train Set molecules</strong> (e.g. <i>Propanolol</i>, <i>Atenolol</i>): 
                Because the exact compound was in the training data, its nearest match is <strong>itself</strong>, giving a similarity of <strong>1.00 (100% identity)</strong>.
              </li>
              <li>
                <strong>Test Set / Novel molecules</strong> (e.g. <i>Ondansetron</i>, <i>Etoposide</i>): 
                Held-out unseen structures yield decimal similarity values (e.g., <strong>0.26</strong>, <strong>0.65</strong>, <strong>0.76</strong>) representing scaffold proximity.
              </li>
            </ul>
          </div>

          {/* Abstention Policy */}
          <div className="callout-box" style={{ borderLeftColor: '#f59e0b' }}>
            <div className="callout-title" style={{ color: '#fbbf24' }}>
              <span>🛡️</span> The Heuristic Abstention Policy (UNCERTAIN)
            </div>
            <p>
              To protect against high-confidence out-of-domain hallucinations, the screening engine will flag a molecule as <strong>UNCERTAIN</strong> if any of these conditions trigger:
            </p>
            <ol style={{ paddingLeft: '20px', lineHeight: '1.6' }}>
              <li><strong>Model Disagreement</strong>: Fewer than 3 out of 4 models agree on the binary threshold (0.50).</li>
              <li><strong>Out-of-Domain Scaffold</strong>: Nearest training Tanimoto similarity is below <strong>0.35</strong>.</li>
              <li><strong>Low Confidence Boundary</strong>: Ensemble mean probability falls within the ambiguous <strong>[0.35, 0.65]</strong> interval.</li>
            </ol>
          </div>

          {/* Disclaimer */}
          <div className="callout-box" style={{ borderLeftColor: '#ef4444' }}>
            <div className="callout-title" style={{ color: '#f87171' }}>
              <span>⚠️</span> Scientific & Clinical Notice
            </div>
            <p>
              Probabilities generated by this system are uncalibrated structural screening estimates. 
              Attention weights from GAT provide qualitative inspection, not causal chemical mechanisms. 
              Wet-lab in-vitro or in-vivo assays are mandatory prior to any pharmacological application.
            </p>
          </div>
        </div>

        <div className="modal-footer">
          <button className="btn btn-primary" onClick={onClose}>
            Understood
          </button>
        </div>
      </div>
    </div>
  );
}
